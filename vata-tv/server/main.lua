-- A basic server script skeleton for future server-side logic if needed.

-- You can add ESX or QBCore integration here if needed in the future
-- Example:
-- local ESX = nil
-- TriggerEvent('esx:getSharedObject', function(obj) ESX = obj end)
